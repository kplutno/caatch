environment: prod

backend:
  replicas: 3
  image:
    tag: prod-stable

frontend:
  replicas: 3
  image:
    tag: prod-stable

postgres:
  enabled: false
  host: postgres-prod.example.com
  port: 5432
  user: postgres_user
  password: postgres_password
  database: caatch_prod
